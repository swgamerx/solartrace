
import { stringToHex } from 'solartrace/helpers/string-to-hex';
import { module, test } from 'qunit';

module('Unit | Helper | string to hex');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = stringToHex([42]);
  assert.ok(result);
});

